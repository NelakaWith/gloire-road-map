# Example .env for backend (do NOT commit secrets to git)
# Copy this file to .env and fill in real values for your environment.

# Server
PORT=3005
NODE_ENV=development

# CORS - Comma-separated list of allowed origins
# Production: Specify exact domains (e.g., https://yourdomain.com,https://www.yourdomain.com)
# Development: Leave empty or use localhost for automatic allowance
CORS_ORIGIN=

# Database (MySQL) - configure according to your local DB
DB_HOST=localhost
DB_NAME=
DB_USER=
DB_PASSWORD=

# JWT secret used to sign auth tokens
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
